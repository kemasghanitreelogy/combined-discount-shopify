query Input {
  cart {
    utmAttribute: attribute(key: "utm_campaign") {
      value
    }
    deliveryGroups {
      deliveryOptions {
        handle
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app", key: "function-configuration") {
      jsonValue
    }
  }
}
